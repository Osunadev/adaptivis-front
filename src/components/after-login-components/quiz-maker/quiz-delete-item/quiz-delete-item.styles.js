import styled from 'styled-components';

export const DeleteItemContainer = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: flex-end;
`;

export const RemoveTextSpan = styled.span`
  font-size: 18px;
  font-weight: bold;
  text-decoration: underline;
  cursor: pointer;
  margin-right: 8px;
`;
